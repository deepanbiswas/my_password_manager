# Generated from template during deployment; chmod 600 on the target host.
ADMIN_TOKEN={{ADMIN_TOKEN}}
DOMAIN={{DOMAIN}}
BACKUP_ENCRYPTION_KEY={{BACKUP_ENCRYPTION_KEY}}
# Must match the name in `rclone config` (see docs/rclone-google-drive-service-account.md for service account setup)
RCLONE_REMOTE_NAME=gdrive
BACKUP_RETENTION_DAYS=30
