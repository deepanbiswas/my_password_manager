# Generated from template during deployment; chmod 600 on the target host.
ADMIN_TOKEN={{ADMIN_TOKEN}}
DOMAIN={{DOMAIN}}
BACKUP_ENCRYPTION_KEY={{BACKUP_ENCRYPTION_KEY}}
# Must match the name in `rclone config` (see docs/rclone-google-drive.md)
RCLONE_REMOTE_NAME=gdrive
# Folder on the remote (create once with rclone mkdir). Default matches backup.sh.template.
RCLONE_BACKUP_FOLDER=vaultwarden-backups-hetzner
BACKUP_RETENTION_DAYS=30
