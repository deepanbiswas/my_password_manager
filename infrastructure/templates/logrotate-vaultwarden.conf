# Vaultwarden stack log files (health checks, backups). Installed to /etc/logrotate.d/vaultwarden by deploy-to-vm.sh (iteration 7).
/var/log/vaultwarden-*.log {
  daily
  rotate 30
  compress
  delaycompress
  missingok
  notifempty
  copytruncate
}
