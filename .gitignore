# Environment variables and secrets
.env
*.env
.env.local
.env.*.local

# Terraform state files
*.tfstate
*.tfstate.*
.terraform/

# Local Terraform variables (copy from terraform.tfvars.example)
**/terraform.tfvars

# Ephemeral CI validation (tdi-quality workflow / local test)
infrastructure/terraform/.ci-terraform/
infrastructure/terraform/azure/.ci-terraform/
infrastructure/terraform/hetzner/.ci-terraform/

# Backup files
backups/
*.backup
*.bak

# Logs
*.log
logs/

# OS files
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# IDE files
.vscode/
.idea/
*.iml

# Temporary files
tmp/
temp/
*.tmp

# Docker volumes (if any)
docker-volumes/

# SSH keys (if stored locally)
*.pem
*.key
id_rsa
id_rsa.pub

# Rclone config (contains credentials)
.rclone.conf

# GPG keys (if stored locally)
*.gpg
*.asc
private-keys/

# Scratchpad (personal notes)
scratchpad.md

# Vibe-coding experience article (local / Confluence source; omit from repo if preferred)
docs/vibe-coding-first-experience.md

# Local GitHub secrets file for scripts/push-github-secrets.sh
scripts/github-secrets.env
scripts/.azure-github-credentials.json

#temporary files - DELETE temp files after use
temp/
