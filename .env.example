# Vaultwarden Environment Variables Configuration Template
# Copy this file to .env and fill in the actual values
# IMPORTANT: Never commit .env to version control

# =============================================================================
# VAULTWARDEN CONFIGURATION
# =============================================================================

# Admin Panel Access Token
# Generate a strong random token: openssl rand -base64 48
# Required for accessing /admin endpoint
ADMIN_TOKEN=<generate-strong-random-token>

# Domain Configuration
# Full domain URL with https:// protocol
# Example: https://vaultwarden.example.com
DOMAIN=https://your-domain.com

# =============================================================================
# BACKUP CONFIGURATION
# =============================================================================

# Backup Encryption Key
# Option 1: GPG Passphrase (recommended for simplicity)
#   Generate: openssl rand -base64 32
#   Store securely (password manager, secure note)
# Option 2: GPG Key ID (for key-based encryption)
#   Generate key: gpg --full-generate-key
#   Use key ID from: gpg --list-keys
BACKUP_ENCRYPTION_KEY=<gpg-key-id-or-passphrase>

# Rclone Remote Name
# Name of the Rclone remote configured for Google Drive
# Configure via: rclone config
# Default: gdrive
RCLONE_REMOTE_NAME=gdrive

# Backup Retention Policy
# Number of days to keep backups in Google Drive
# Backups older than this will be automatically deleted
# Default: 30 days
BACKUP_RETENTION_DAYS=30

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Alert Email (Optional)
# Email address for health check alerts
# Leave empty if not using email alerts
# ALERT_EMAIL=your-email@example.com

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 
# 1. Keep .env file permissions restricted: chmod 600 .env
# 2. Never commit .env to version control
# 3. Backup encryption key separately (password manager, secure note)
# 4. Rotate ADMIN_TOKEN periodically
# 5. Store backup encryption key securely - you'll need it for restore
#
# =============================================================================
# GENERATION INSTRUCTIONS
# =============================================================================
#
# ADMIN_TOKEN:
#   openssl rand -base64 48
#
# BACKUP_ENCRYPTION_KEY (Passphrase):
#   openssl rand -base64 32
#
# BACKUP_ENCRYPTION_KEY (GPG Key):
#   gpg --full-generate-key
#   gpg --list-keys  # Use the key ID from output
#
# =============================================================================
